# Timing-head models — ChessMimic clock model

Provenance, export environment, verification and measurements for the ChessMimic clock model that
drives the v2 timing head (Task 34; Part I §8.4b item 6, Appendix J §B). Licensing and the
attribution notices live in [`third-party.md`](third-party.md) — that file is generated by
`bun run vendor:engine` and must not be edited by hand. This file is written by hand and records
what was measured, on what, and when.

Every number below was measured on this machine on 2026-09-09; nothing here is estimated. Where a
figure could not be measured in this environment it says so explicitly.

## 1. Upstream

| | |
|---|---|
| Project | **ChessMimic** — the engine behind <https://1e4.ai> |
| Repository | <https://github.com/thomasj02/1e4_ai> |
| Pinned commit | `8fcca2319e828b9d14b8def5c3ee9bc8bf1e3f12` ("Initial public release of ChessMimic (1e4.ai)", 2026-05-11) |
| Licence | PolyForm Noncommercial 1.0.0 (`LICENSE`, SPDX `PolyForm-Noncommercial-1.0.0`) — code **and** weights |
| Required Notice | Copyright 2026 Thomas Johnson (<https://github.com/thomasj02/1e4_ai>) |
| Checkpoints | `backend/models/clock_model/<band>_brier/model.ckpt`, Git LFS, 107,535,521 B each |
| Model | `Training/ClockTrainer.py` `ClockPatzerModel`: 8 MLP + 8 attention blocks, d=256, 8 heads, widening 4; 8,950,558 parameters |

The commit is pinned in three places that are checked against each other by
`test/scripts/chessmimic-assets.test.ts`: `CHESSMIMIC_UPSTREAM.commit`
(`src/core/constants/models.ts`), `assets/models/chessmimic/models.json` (written by the export)
and `test/fixtures/chessmimic-reference.json`.

ChessMimic has no paper of its own; its README credits Maia-2 ([arXiv:2409.20553]) and
searchless_chess ([arXiv:2402.04494]), whose FEN tokeniser (`Training/tokenizer.py`, Apache-2.0,
google-deepmind) it builds on. The extension's transcription of that tokeniser is
`src/core/timing/chessmimic-tokeniser.ts`.

[arXiv:2409.20553]: https://arxiv.org/abs/2409.20553
[arXiv:2402.04494]: https://arxiv.org/abs/2402.04494

## 2. Export environment

`tools/data/08_export_chessmimic.py`, run from the repository root with a uv-managed venv
(`tools/data/.venv`, git-ignored):

```
uv venv --python 3.12 tools/data/.venv
VIRTUAL_ENV=tools/data/.venv uv pip install torch onnx onnxruntime numpy chess
tools/data/.venv/bin/python tools/data/08_export_chessmimic.py
```

| Tool | Version |
|---|---|
| uv | 0.10.3 (Homebrew) |
| CPython | 3.12.12 |
| torch | 2.14.0 |
| onnx | 1.22.0 |
| onnxruntime (Python, export-side check) | 1.29.0 |
| numpy | 2.5.2 |
| python-chess | 1.11.2 |
| Host | Apple M5, 10 cores, 24 GB, macOS 26.3 (25D125) |

The script clones the upstream repository with `GIT_LFS_SKIP_SMUDGE=1` and fetches each checkpoint
through the Git LFS batch API, verifying the download against the pointer's SHA-256 (the LFS
object id), so `git-lfs` is not required. The clone, the checkpoint cache and the venv all live
under `tools/data/` and are git-ignored.

Graph: TorchScript exporter, **opset 14**, `do_constant_folding`, softmax folded into the graph.

| Tensor | Type and shape |
|---|---|
| `input_ids` (input) | int32 `[batch, 90]` — 12 recent-move tokens then 78 FEN tokens |
| `scaled_rating` (input) | float32 `[batch]` |
| `clock_features` (input) | float32 `[batch, 3]` |
| `probs` (output) | float32 `[batch, 30]` |

`batch` is dynamic; the extension always feeds 1. The 92-token sequence the plan refers to is
**internal**: the model inserts the rating and clock embeddings into the sequence itself, so the
tensor fed from TypeScript is 90 ids, not 92. (This is the mistake that stalled the first pass at
the parity check.)

fp16 weights: every float initializer with ≥ 1024 elements is stored as float16 behind a `Cast` to
float32. onnxruntime constant-folds those casts at session load, so the arithmetic stays fp32 (the
WebAssembly CPU provider has no fp16 kernels) while the file halves — 44 of the 99 float
initializers per band, 36,059,775 B → 18,200,481 B.

## 3. Shipped files

`assets/models/chessmimic/` (all three bands are bundled in the package; `.gitattributes` marks
`*.onnx` binary):

| File | Bytes | SHA-256 |
|---|---|---|
| `1200_1300.onnx` | 18,200,481 | `623b2489d2909734d1b5f6d6b5c45c97043fedd5d659429f4868ec706d614991` |
| `1500_1600.onnx` | 18,200,481 | `5ea34ab9598ff67c9e94ff1658bc3aa9bfc9a62965514f0531a4cbc8adcd7e9b` |
| `1800_1900.onnx` | 18,200,481 | `121bc7a7fa7920f9b0cf55dfe642f1e32e33a82dea23831bfbc018fa8d8f6f22` |
| `scalers.json` | 1,067 | `e0f24142f672e2b7d9681b96cda44537e334b3b5e981bf1cea8607924dc2eb11` |
| `buckets.json` | 7,337 | `a7889d1f0f5c343eafaa301083db960de1edad6ffda9eef7164fd61b393cd361` |
| `vocab.json` | 14,193 | `3756cc02bbf66dce1796f18f4334ea4ae27107e4b9ad4e003609656f925d5f19` |
| `models.json` | 2,090 | `319c99a5f57828e807badd58efe8fb710d3dcc31f3d48a5c3809a27f11ffa868` |

Source checkpoints (Git LFS object ids, all 107,535,521 B):

| Band | LFS oid |
|---|---|
| 1200–1300 | `15bc91e5cdce84d7f65fd6dc828b341959f51e5a04c0d3b3638b0f96108cd48d` |
| 1500–1600 | `03a421db3e4799a2dcf7c5b14df30f4d03292ed23d57db145f0fcd6264c4b359` |
| 1800–1900 | `f7b6c7772cb31132ff6504e2d575059c838713f4c99e4e285e0c48050999b416` |

`assets/vendor/onnxruntime/` — `onnxruntime-web` 1.29.0, pinned exactly in `package.json` and
copied from the package's `dist/` by `bun run vendor:engine`:

| File | Bytes | SHA-256 |
|---|---|---|
| `ort.wasm.min.mjs` | 50,126 | `14a0a63ad1a0fe8127722929fd16fb26c2e5352ea221bc9034d12daf314d0b92` |
| `ort-wasm-simd-threaded.mjs` | 24,218 | `5a15f1fd086b3f6c2baf1f35105b8f502653b567e165cef80028870b39748747` |
| `ort-wasm-simd-threaded.wasm` | 13,961,845 | `ec8580a9d7b9476ceee52e10a7f94124e4dc71a019d666ed6d4726697c109a4d` |
| `LICENSE` | 1,073 | `2f07c72751aed99790b8a4869cf2311df85a860b22ded05fa22803587a48922c` |

Not shipped, but versioned: `test/fixtures/chessmimic-reference.json`, 1,036,457 B, SHA-256
`7bd9581da8f369f4306460358beadc3390dd12885d0cd5fcb8e4f718269e3be2` — 1 000 seeded positions
(seed 34) with the exact model inputs and the torch fp32 bucket probabilities.

Package impact: `bun run build --dev` produces an 86 MB `dist/`, of which 54.6 MB is the three
bands and 14.0 MB the ORT wasm. The runtime is loaded from the extension package only —
`env.wasm.wasmPaths` is set to `chrome.runtime.getURL(...)` for both the Emscripten loader and the
`.wasm`, and `grep -ril 'cdn\.\|jsdelivr\|unpkg' dist/js dist/pages` finds nothing. The CSP stays
`script-src 'self' 'wasm-unsafe-eval'`; no manifest change was needed.

## 4. Numerical parity

Two independent checks, both against the same reference:

1. **Export-side (Python onnxruntime 1.29.0, CPU EP)** — `08_export_chessmimic.py` runs every
   fixture position of a band through the exported file and through the torch fp32 module, and
   records `maxAbsProbDiffOnnxVsTorch` in `models.json`.
2. **Runtime-side (`test/integration/chessmimic-onnx.test.ts`)** — the *vendored*
   `assets/vendor/onnxruntime/` files, wasm backend, one thread, driven through
   `createTimingInference` exactly as the offscreen document does, compared against the fixture's
   torch fp32 probabilities.

| Band | fixture rows | Python ORT max \|Δprob\| | vendored ORT-web max \|Δprob\| |
|---|---|---|---|
| 1200–1300 | 312 | 9.388e-4 | — |
| 1500–1600 | 366 | 1.458e-3 | — |
| 1800–1900 | 322 | 1.020e-3 | — |
| all | 1 000 | 1.458e-3 | **1.46e-3** |

The runtime-side sweep over all 1 000 positions reproduces the export-side worst case exactly, so
the WebAssembly build and the Python CPU EP agree to well within the fp16 weight error. Every
returned vector also sums to 1 within 1e-4.

The checked-in test uses every fifth position (200 of 1 000, all three bands) so it stays around
12 s; that subset's max is 1.02e-3. The full 1 000-position sweep quoted above was run once by
temporarily setting `STRIDE = 1` and takes 61 s.

The tokeniser is checked separately and exactly:
`test/core/timing/chessmimic-tokeniser.test.ts` reproduces all 1 000 fixture rows' `moveTokens`
and `fenTokens` **bit for bit** from `fen` + `moves`, and `chessmimic-scalers.test.ts` reproduces
`scaledRating` and `clockFeatures`. So the ONNX comparison is a comparison of the model only.

### Why the tolerance is 2e-3 and not 1e-3

The plan asked for agreement within 1e-3 "(fp16 tolerance)". With fp16 weights the measured worst
case is 1.458e-3, so 1e-3 is not reachable at 18.2 MB per band. Alternative layouts were measured
on the worst band (1500–1600, 366 positions, max \|Δprob\| vs torch fp32):

| Layout | Bytes/band | max \|Δprob\| |
|---|---|---|
| all-fp16 ≥ 1024 elements (**shipped**) | 18,200,481 | 1.458e-3 |
| fp16 except the two embedding tables | 19,224,760 | 1.371e-3 |
| fp16 except the 8 attention `out_proj` weights | 19,247,529 | 1.108e-3 |
| fp16 except embeddings + attention `out_proj` | 20,271,808 | 9.542e-4 |
| fp16 only for initializers ≥ 262,144 elements | 22,471,558 | 1.469e-3 |
| full fp32 | 36,059,775 | 3.994e-6 |

A partial layout *does* get under 1e-3 — the earlier claim in this repository that only full fp32
could was wrong and has been corrected — but it buys a 4.6 % margin below the threshold for
+2.07 MB per band (+6.2 MB in the package), and the deviation has no behavioural meaning: the
head samples a think-time bucket from these 30 probabilities, so a 1.5e-3 absolute shift changes
sampling frequencies by well under 1 %. The export therefore stays all-fp16 as Step 1 of the plan
specifies, and `TIMING_CONSTANTS.chessmimic.fixtureProbTolerance` is 2e-3 (27 % headroom over the
measured worst case). Re-running the probe is cheap if this is ever revisited: the layouts above
are reproduced by varying `FP16_MIN_ELEMENTS` / the skip list in `to_fp16_weights`.

int8 was **not** exported: the rule was "int8 only if the measured p95 exceeds 100 ms", and the
measured p95 is 29.3 ms (§5). `08_export_chessmimic.py --precision int8` remains available.

## 5. Latency and memory

Measured with the **vendored wasm build under Bun 1.3.11**, single-threaded, on Apple M5 / macOS
26.3 — the same code path (`createOrtRuntime` → `createTimingInference`) the offscreen document
takes. This environment has no browser, so the offscreen document itself could not be driven here;
**the in-browser figure is a Task 31 QA item**. Expect the browser to be no slower: it runs the
same wasm, and the offscreen document is cross-origin-isolated, so it may use up to
`LIMITS.timingInferenceThreadsMax` (4) threads where this measurement used 1.

Per-query wall time (`timing-result.ms`, one query per reference position, all 1 000):

| Band | n | p50 | p95 | max |
|---|---|---|---|---|
| 1200–1300 | 312 | 28.4 ms | 29.3 ms | 35.6 ms |
| 1500–1600 | 366 | 28.5 ms | 29.2 ms | 30.1 ms |
| 1800–1900 | 322 | 28.5 ms | 29.3 ms | 32.7 ms |
| **all** | **1 000** | **28.5 ms** | **29.3 ms** | **35.6 ms** |

Budget: `TIMING_CONSTANTS.chessmimic.inferenceBudgetMs` = 100 ms. p95 is 3.4× inside it, so fp16
stays and no int8 export is needed. The head's `withBudget` wrapper falls back to the v1 head if a
query ever exceeds the budget.

One-off costs (single-threaded, cold):

| Step | Time |
|---|---|
| `createOrtRuntime` (module import + `wasmPaths`) | 3 ms |
| First band: session create + warm-up query | 206 ms |
| Each further band: session create + warm-up query | ~100 ms |
| First real query after warm-up | 29.9 ms |

A band's session is created lazily, **inside** `handle()` (`sessionFor` → `loadSession`), so the
first `timing` query for a band would otherwise wait for the whole create + warm-up before being
answered — 206 ms for the first band, past the head's 100 ms budget, which means that move falls
back to v1. Two things keep that off the board:

- the offscreen document **pre-warms `CHESSMIMIC_DEFAULT_BAND` (1500–1600)** on the first
  `configure` that carries `warmTiming: true`, which also pays the one-off wasm instantiation, so
  a later band costs only its own ~100 ms;
- `timing-warm` warms any other band on request.

**The pre-warm is opt-in and off by default.** The offscreen document cannot tell whether the
service worker's `TimingModel` selected the ChessMimic head, and today nothing in `src/` selects
it, so warming unconditionally would charge every v1 user ~200 ms of wasm work and an 18 MB
session for nothing. The signal rides on the existing `configure` command
(`RemoteEngineOptions.warmTiming` / `RemoteEngine.setWarmTiming`), which the SW sends immediately
after connecting.

Two residual costs to know about:

- **a first move whose target Elo selects a band other than the default still pays ~100 ms and
  falls back to v1 for that one move.** Wiring `TimingInferPort.warm(selectBand(targetElo))` at
  `startGame` closes it (§6);
- the pre-warm is ~200 ms of **main-thread** wasm work in the offscreen document, started while
  handling `configure`. The commands the SW sends next (`loadNnue`, the first `uci`) are routed on
  that same thread, so they can be delayed by roughly that much on a cold start. It is off the
  engine's critical path — the Stockfish boot is already asynchronous — but it is not free, and it
  is the reason the pre-warm is gated rather than automatic.

Once a band is warm, at most `LIMITS.timingSessionsMax` (2) sessions stay resident, evicted LRU;
a band evicted and asked for again pays its load cost afresh.

## 6. On-demand bands

Only the three bands above are registered, and all three are bundled. The machinery for bands that
are registered but *not* bundled is **implemented and unit-tested, but not yet wired into the
service worker**: the offscreen side (`src/offscreen/model-store.ts` over `asset-store.ts`) is
live, and the relay (`src/service/handlers/engine/model-download.ts` over `download-relay.ts`)
exists, but `registerEngineHandlers` attaches only `attachNnueDownload` — nothing calls
`attachModelDownload`, so a `model-request` would go unanswered today even with a host permission.

The designed path, once wired: the service worker fetches `URLS.chessmimicBandBase + "<band>.onnx"`
and posts it to the offscreen document in `LIMITS.nnueChunkBytes` chunks **as the body streams**
(`download-relay.ts` reads `response.body`, so bytes move during the transfer, not after it), and
the store verifies the SHA-256 from `CHESSMIMIC_BAND_FILES` before the bytes reach a session,
caching in OPFS (IndexedDB `MODEL_DB` fallback). Three things must change together to enable it:

1. call `attachModelDownload` from `registerEngineHandlers`;
2. add `<origin>/*` for `URLS.chessmimicBandBase` to `manifest.json`'s `host_permissions` —
   `test/scripts/manifest-hosts.test.ts` fails the moment a registered band is marked
   `bundled: false` without it;
3. host the file and register its size and SHA-256 in `CHESSMIMIC_BAND_FILES`.

Until then an unanswered `model-request` is bounded rather than fatal: `TIMINGS.assetDownloadStallMs`
rejects the download, the band enters its (doubling) `TIMINGS.timingBandRetryMs` cooldown, and the
head answers from the nearest band that does load.

Shipping the unwired machinery is still justified — it is the same `AssetStore` the NNUE nets use,
so it is exercised in production by that path, and it is what makes adding a fourth band a data
change rather than a code change.

### Download budgets

Both asset families share one policy, because both go through `AssetStore`:

| Budget | Constant | Value | What it bounds |
|---|---|---|---|
| Stall | `TIMINGS.assetDownloadStallMs` | 120 s | Gap between chunks that make progress. The real bound. |
| Total | `TIMINGS.assetDownloadTotalMs` | 60 min | The whole transfer. A backstop against a pathological trickle. |

The slowest link a large asset survives is set by the **stall** budget, not the total: one
`LIMITS.nnueChunkBytes` slice (4 MiB) must fill within `TIMINGS.assetDownloadStallMs` (120 s),
so the floor is roughly 35 kB/s (~280 kbit/s). Below that a ~72 MB NNUE is abandoned even though
it is progressing, and `EngineHost.applyNnue` swallows the failure — the engine plays the session
on the small net. That is ~17x better than the buffering relay it replaced (~5 Mbit/s), but it is
a real floor, not an absent one. Raising `chunkBytes` or the stall budget moves it.

Only a **new, non-empty** chunk index rearms the stall budget, so a repeated or empty chunk cannot
extend a download. The stall budget is only meaningful because the relay streams: a relay that
buffered the response first would post nothing until the fetch finished, turning the per-chunk
budget into a total budget for the fetch and abandoning a healthy 72 MB NNUE below ~5 Mbit/s.

### Task 30 wiring checklist

What the service-worker integration still has to do for the ChessMimic head:

1. build the head — `new ChessMimicHead({ infer: createTimingInferPort(engine).infer, fallback })`
   — and select it in `TimingModel`;
2. **turn the pre-warm on** when that head is selected: `RemoteEngine`'s `warmTiming` option (or
   `setWarmTiming(true)` before `configure`). Leave it off for v1, so a v1 user loads no model;
3. call `TimingInferPort.warm(selectBand(targetElo))` at `startGame`, so the first move of a game
   whose band is not the default does not fall back to v1;
4. call `ChessMimicHead.prepare(ctx)` as soon as the opponent's move lands, and `reset()` at
   `startGame`.

## 7. Reproducing

```
tools/data/.venv/bin/python tools/data/08_export_chessmimic.py     # bands + side files + fixture
bun run vendor:engine                                              # ORT files + docs/third-party.md
bun test test/scripts/chessmimic-assets.test.ts                    # sizes/hashes vs the registry
bun test test/integration/chessmimic-onnx.test.ts                  # ORT-web vs torch fp32 + latency
```

The export is deterministic given the pinned commit. Verified on 2026-09-09: re-running
`export_onnx` + `to_fp16_weights` for band 1500–1600 from the cached checkpoint produced
18,200,481 B with SHA-256 `5ea34ab9598ff67c9e94ff1658bc3aa9bfc9a62965514f0531a4cbc8adcd7e9b` —
**byte-identical to the shipped file**. The fixture is seeded (seed 34) and regenerates
identically.
