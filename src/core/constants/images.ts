/** Extension-packaged images, as `chrome.runtime.getURL` paths (Task 23: the brand mark). */
export const IMAGES = {
	/** 256 px brand mark; rendered at 64 px on login/update and 24 px in the top bar. Never recoloured. */
	mark: "assets/images/sliced_256.png",
} as const;
