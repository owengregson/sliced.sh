/**
 * tools/human-match/maia.ts — the Maia runner at its historical path; it lives in
 * `tools/lib/maia.ts`. Nothing here runs in the extension.
 */

export { createMaiaRunner, type MaiaRunner, maiaModelBytes } from "../lib/maia";
