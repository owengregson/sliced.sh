"""The ChessMimic finetune's shared parts: chess.com PGN reading (`pgn`), one game to its encoded
examples (`rows`), the player-level selection of moves (`sides`), and the held-out evaluation's
metrics, cells, model runs and report (`metrics`, `cells`, `models`, `report`). The encoder
mirror is `cmenc.py` and the network `model.py`, beside this package.
"""
